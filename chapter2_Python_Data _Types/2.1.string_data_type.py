"""
Data types are nothings but variables you use for reserve space in memory.python variables  does not 
need an explicit declaration to reserve a memory space . the declaration happens autoatically when you assign a value to a variable
"""

# string data types 
# strings are identified as a contiguous of characters in the quotation makes.

myString = "Hello i am tapendra bista "
print(myString)
print(myString[0])
# just print 5 character of the string 
print(myString[0:5])